spring:
  application:
    name: thrift-client
  main:
    web-environment: false

info:
  project:
    version: #project.version#

#===============================================
# Application specific properties
#===============================================
application:
  server:
    ip: localhost
    port: 9090
  interval: 1500