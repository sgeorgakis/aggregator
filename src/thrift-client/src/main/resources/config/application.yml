info:
  project:
    version: #project.version#

spring:
  application:
    name: thrift-client
  main:
    web-environment: false
  profiles:
    active: dev

#===============================================
# Application specific properties
#===============================================
application:
  server:
    ip: localhost
    port: 9090
  interval: 1500